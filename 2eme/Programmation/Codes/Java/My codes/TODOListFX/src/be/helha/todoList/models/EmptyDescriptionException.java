package be.helha.todoList.models;

public class EmptyDescriptionException extends RuntimeException {
    // This error is thrown when you try to add an item to the todo list but there
    // is no description.
}
